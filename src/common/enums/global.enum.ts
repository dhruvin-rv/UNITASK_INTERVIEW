export enum HEADERS {
  AUTH_USER_DETAILS = 'x-auth-user-details',
}

export enum PAYLOAD_RES_STATUS {
  RESPONSE_PAYLOAD_STATUS_SUCCESS = 1,
  RESPONSE_PAYLOAD_STATUS_ERROR = 0,
  RESPONSE_PAYLOAD_STATUS_WARNING = 2,
  RESPONSE_PAYLOAD_STATUS_NOT_VERIFIED = 3,
}

export enum JWT {
  SECRET = 'jokelovejoke',
}

export enum RESPONSE_TYPE {
  FAIL = 'fail',
  SUCCESS = 'ok',
  AUTH_FAIL = 'auth_fail',
}
